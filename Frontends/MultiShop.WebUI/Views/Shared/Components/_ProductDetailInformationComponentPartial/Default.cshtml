@model GetByIdProductDetailDto

<div class="ms-tab-pane hidden" id="tab-pane-2">
    <h4 class="text-headline-lg-mobile font-headline-lg-mobile text-on-surface mb-3">@Shared["ProductInfoTab"]</h4>
    <p class="text-body-md text-secondary leading-relaxed">@(Model?.ProductInfo)</p>
</div>
