@model GetByIdProductDetailDto

<div class="ms-tab-pane hidden" id="tab-pane-1">
    <h4 class="text-headline-lg-mobile font-headline-lg-mobile text-on-surface mb-3">@Shared["ProductDetailTab"]</h4>
    <p class="text-body-md text-secondary leading-relaxed">@(Model?.ProductDescription)</p>
</div>
